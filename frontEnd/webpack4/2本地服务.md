

```js
npm install webpack-dev-server -D
```

```javascript
devServer: {
    port: 3000,
    host: 'localhost',
    progress: true,
    contentBase: './build',
    open: true,
    compress: true // gzip压缩
    
}
```

